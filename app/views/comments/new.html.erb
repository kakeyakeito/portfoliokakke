<%= render "layouts/user_header"%>
<div class="container">
	<div class="row">
		<h1>Comment To Title</h1>
		<%= form_with(model:Comment.new, url: post_comments_path, local:true) do |f| %>
		<div class="field commentform">
			<h2 class="commenttitle">comment</h2>
			<%= f.text_field :comment %>
			<%= f.submit 'comment' %>
			<% end %>
		</div>
	</div>
</div>