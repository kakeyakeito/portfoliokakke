<%= form_for(current_user.blocking_block_relationships.build(blocking_id: @user.id)) do |f| %>
  <div><%= f.hidden_field :blocking_id %></div>
  <%= f.submit "Block", class: "btn btn-large btn-primary follow-btn" %>
<% end %>